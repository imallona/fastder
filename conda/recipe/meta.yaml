package:
  name: fastder
  version: "0.0.1"

source:
  path: ../..   # points to the root of the repo

build:
  number: 0
  script: bash build.sh

requirements:
  build:
    - cmake >=4.0
    - make
    - gcc
    - gxx
  run:
    - libstdcxx-ng

about:
  home: "https://github.com/imallona/fastder"
  license: "MIT"
  summary: "fastder, a faster expressed regions finder for RNA-seq"
